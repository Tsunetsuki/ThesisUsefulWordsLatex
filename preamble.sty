\ProvidesPackage{preamble}
\usepackage[dvipsnames, table,xcdraw]{xcolor}
\usepackage{booktabs}
\usepackage[expansion=false]{microtype}
\usepackage{silence}

\WarningFilter{hyphenat}{*}
\usepackage{etoolbox}
% Suppress font warnings
\def\@font@warning#1{}

\usepackage[htt]{hyphenat}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{pgfplots}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{natbib}
\usepackage{graphicx}
\usepackage{listings}
\usepackage{multirow} 
\usepackage{tikz, tkz-euclide}
\usepackage{physics}
\usepackage{amsmath, amssymb}
\usepackage{float}
\usepackage{blindtext}
\usepackage{caption}
\usepackage{droidsansmono}
\usepackage{titlesec}
\usepackage{hyperref}
\usepackage{xfrac}
\usepackage{fancyhdr}
\usepackage[a4paper,width=150mm,top=25mm,bottom=25mm]{geometry}
\usepackage{geometry}
\usepackage{tabularx}
\usepackage{xurl}
\usepackage{makecell}
\usepackage{parskip}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning}
\setlength{\parindent}{2em}
\pgfplotsset{compat=1.18}  % Use the latest version available
\pagestyle{fancy}

\graphicspath{ {images/} }

\tikzstyle{process} = [rectangle, rounded corners, minimum width=3.5cm, minimum height=1cm,text centered, draw=black, fill=blue!10]
\tikzstyle{arrow} = [thick,->,>=Stealth]
\tikzstyle{data} = [text width=4cm, align=center, font=\small\itshape]

\AtBeginEnvironment{tabular}{\footnotesize} 

\rowcolors{3}{white}{lightgray!20}

\fancyhf{}                              % delete the current header and footer
\fancyhead[LO]{\parbox[t]{0.9\textwidth}{\rightmark}} %<--- use [t] for parbox.
% \fancyhead[RO]{\parbox[t]{0.02\textwidth}{\textbf{\thepage}}} %<-
\fancyfoot[C]{\thepage}
\setlength{\headheight}{25pt}

\newcommand{\tss}{\textsubscript}
\newcommand{\inv}[1]{\frac{1}{#1}}
\newcommand{\invsqrttwo}{\frac{1}{\sqrt{2}}}
\newcommand{\half}[1]{\frac{#1}{2}}
\newcommand{\onehalf}{\frac{1}{2}}
\newcommand{\degree}{^{\circ}}
\newcommand{\argmax}{\mathop{\rm arg~max}\limits}
\newcommand{\argmin}{\mathop{\rm arg~min}\limits}

\newcommand\todo[1]{\textcolor{red}{ToDo: #1}}
\newcommand\footnoteref[1]{\protected@xdef\@thefnmark{\ref{#1}}\@footnotemark}

\newcommand\NLP{Natural Language Processing}
\newcommand\ML{Machine Learning}
\newcommand\AI{Artificial Intelligence}
\newcommand\XAI{Explainable AI}
\newcommand\Rosetta{\textit{Rosetta Stone}}

% set the default code style
\lstset{
	frame=tb, % draw a frame at the top and bottom of the code block
	tabsize=4, % tab space width
	showstringspaces=false, % don't mark spaces in strings
	numbers=left, % display line numbers on the left
	escapeinside={(*}{*)},
	basicstyle=\footnotesize\droidsansmono,
	frame=single,
	commentstyle=\color{ForestGreen}, % comment color
	keywordstyle=\color{blue}, % keyword color
	stringstyle=\color{BrickRed}, % string color
	breaklines=true,
	emphstyle={\color{blue}},
	columns=flexible,
	postbreak=\mbox{\textcolor{red}{$\hookrightarrow$}\space},
}

\pagenumbering{arabic}
% \titleformat*{\section}{\LARGE\bfseries}
% \titleformat*{\subsection}{\Large\bfseries}
% \titleformat*{\subsubsection}{\large\bfseries}
% \titleformat*{\paragraph}{\large\bfseries}
% \titleformat*{\subparagraph}{\large\bfseries}
%
